import { HighlightThreshold } from './highlight-threshold';

describe('HighlightThreshold', () => {
  it('should create an instance', () => {
    const directive = new HighlightThreshold();
    expect(directive).toBeTruthy();
  });
});
